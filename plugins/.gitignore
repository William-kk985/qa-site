# 各语言的编译中间产物 —— 不要提交。
# （prebuilt/ 是例外：那是编译好的成品，**要**提交，好让人不用装工具链就能下载试用。）

# Rust
rust/target/

# TypeScript
ts/.out/

# ReScript
rescript/lib/
rescript/.rescript*

# build.sh 的临时日志
prebuilt/.*.log
__pycache__/

# 体积基准（bench-size.sh）的中间产物和实验产物。
# ⚠️ 这个基准和 prebuilt/ 不一样：它的 .wasm 只是**实验读数**，不需要提交、
#    也不给人下载 —— 谁想看，跑一遍 bench-size.sh 就能原样复现。
bench/rust-fixed/target/
bench/rust-vec/target/
bench/moon/_build/
bench/.out/
